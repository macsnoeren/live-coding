name: deploy
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v3.5.1
        with:
          node-version: '18'
      - run: cd compiler
      - run: npm install
      - run: npm update
      - run: npm test
      
